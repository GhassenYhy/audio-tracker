import { WordEditorDirective } from './word-editor.directive';

describe('WordEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new WordEditorDirective();
    expect(directive).toBeTruthy();
  });
});
