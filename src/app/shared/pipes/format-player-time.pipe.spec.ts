import { FormatPlayerTimePipe } from './format-player-time.pipe';

describe('FormatPlayerTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlayerTimePipe();
    expect(pipe).toBeTruthy();
  });
});
