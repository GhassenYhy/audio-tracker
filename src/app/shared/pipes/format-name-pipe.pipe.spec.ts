import { FormatNamePipePipe } from './format-name-pipe.pipe';

describe('FormatNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
